#!/usr/bin/env bash
#
# No need to use cd anymore
shopt -s autocd

# Autocorrects minor errors on directories names
shopt -s cdspell

# Case-insensitive filename autocompletion using TAB
shopt -s nocaseglob
